{
  "name": "vapi-voice-tour-monorepo",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "packageManager": "pnpm@9.1.0",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "pnpm -r --filter ./packages/** --filter ./apps/** build",
    "dev": "echo 'Use pnpm -F <package> dev'",
    "lint": "eslint .",
    "format": "prettier --write ."
  }
} 